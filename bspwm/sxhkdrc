# Spawn a terminal
alt + Return
	kitty

# Make sxhkd reload its configuration files:
super + r
	pkill -USR1 -x sxhkd

# Spawn a browser
alt + b
	firefox

# Open the task manager
alt + t
	xfce4-taskmanager

# Reset the keyboard layout (I mess it up too often)
shift + space
	setxkbmap us

# Instant screenshot
Print
	flameshot full -p ~/Photos/screenshots/

# Quit/Restart bspwm
super + shift + {q,r}
  bspc {quit,wm -r}

